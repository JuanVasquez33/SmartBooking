namespace SmartBooking.Core.Entities
{
    public enum TipoUsuario { cliente, profesional, administrador }
    public enum EstadoReserva { pendiente, confirmada, cancelada, reprogramada }
    public enum TipoNotificacion { confirmacion, cancelacion, recordatorio, reprogramacion }

}
